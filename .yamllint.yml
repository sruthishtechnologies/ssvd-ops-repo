extends: default

rules:
  line-length:
    max: 140
    level: warning
  document-start: disable
  truthy:
    allowed-values:
      - "true"
      - "false"
      - "on"
      - "off"
  comments:
    min-spaces-from-content: 1
